require 'spec_helper'

describe FactoryDays do
  it 'has a version number' do
    expect(FactoryDays::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
